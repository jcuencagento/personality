/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

module.exports = {
    images: {
        domains: ['image.tmdb.org', 'i.scdn.co', 'pbs.twimg.com']
    }
}